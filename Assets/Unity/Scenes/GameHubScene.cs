using UnityEngine;

namespace Unity.Scenes
{
    public class GameHubScene : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
